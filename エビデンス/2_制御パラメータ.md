# 2_制御パラメータ

## 項目

### idletimeoutで接続が中断されること

### sizelimit で応答が中断されること

### timelimitで応答が中断されること

### readonlyでデータベースが読み込み専用となること

### OpenLDAPにnone権限のユーザーでアクセスし、詳細が表示されずに接続が拒否されること

### OpenLDAPにdisclose権限のユーザーでアクセスし、エラーの詳細が表示され接続が拒否されること

### OpenLDAPにauth権限のユーザーでアクセスし、参照が行えないこと

### OpenLDAPにcompare権限のユーザーでアクセスし、ldapcompareが行えること

### OpenLDAPにread権限のユーザーでアクセスし、ツリーが参照できること

### OpenLDAPにwrite権限のユーザーでアクセスし、他の権限を無視してツリーを参照できること

### OpenLDAPにmanage権限のユーザーでアクセスし、他の権限を無視してツリーを参照できること

### OpenLDAPに管理者権限のあるユーザーでエントリーが追加できること

### OpenLDAPに書き込み権限のあるユーザーでエントリーが追加できること

### OpenLDAPに読み込み権限のあるユーザーでエントリーが追加できないこと

### OpenLDAPに管理者権限のあるユーザーでエントリーが変更できること

### OpenLDAPに書き込み権限のあるユーザーでエントリーが変更できること

### OpenLDAPに読み込み権限のあるユーザーでエントリーが変更できないこと

### OpenLDAPに管理者権限のあるユーザーでエントリーが削除できること

### OpenLDAPに書き込み権限のあるユーザーでエントリーが削除できること

### OpenLDAPに読み込み権限のあるユーザーでエントリーが削除できないこと

### dn= のACLでアクセス制御が行えること

### dn.base= のACLでアクセス制御が行えること

### dn.subtree= のACLでアクセス制御が行えること

### dn.children= のACLでアクセス制御が行えること

### dn.regex= のACLでアクセス制御が行えること

### anonymous のACLでアクセス制御が行えること

### users のACLでアクセス制御が行えること

### self のACLでアクセス制御が行えること

### peername= のACLでアクセス制御が行えること

### group=のACLでアクセス制御が行えること

### filter= のACLでアクセス制御が行えること

### attrs= のACLでアクセス制御が行えること

### plaintextのパスワードを設定し、認証利用できること

### cryptのパスワードを設定し、認証利用できること

### MD5のパスワードを設定し、認証利用できること

### SMD5のパスワードを設定し、認証利用できること

### SHAのパスワードを設定し、認証利用できること

### SSHAのパスワードを設定し、認証利用できること

### PBKDF2のパスワードを設定し、認証利用できること

### SSHA256のパスワードを設定し、認証利用できること

### SSHA512のパスワードを設定し、認証利用できること

### ldappasswd実行時、password-hash に設定した形式でハッシュ化されることを確認

### よみがな設定が正常に動作すること
